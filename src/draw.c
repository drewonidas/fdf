#include "fdf.h"



static void			draw_line(t_point *point, t_gen *generator)
{
	
}

static void			draw_point(t_point *point, t_gen *generator)
{
	mlx_pixel_put(point->x, point->y);
}

void				draw_img(t_gen *generator)
{
		
}
